name: Say Hello

on: 
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "Hello, Simon! Your workflow is running successfully 🚀"
